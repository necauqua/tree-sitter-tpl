{
  "name": "tree-sitter-tpl",
  "version": "0.1.0",
  "main": "bindings/node",
  "scripts": {
    "build": "tree-sitter generate && npm install",
    "test": "tree-sitter test"
  },
  "dependencies": {
    "nan": "^2.17.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.20.7"
  },
  "tree-sitter": [
    {
      "scope": "source.tpl",
      "file-types": [
        "tpl"
      ],
      "injection-regex": "tpl"
    }
  ]
}
